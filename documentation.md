[comment]: <> (Code generated by mdatagen. DO NOT EDIT.)

# iceberg

## Internal Telemetry

The following telemetry is emitted by this component.

### otelcol_exporter_iceberg_buffer_flush_duration_seconds

Duration of flush attempts for a table.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| s | Histogram | Double | Alpha |

#### Attributes

| Name | Description | Values | Semantic Convention |
| ---- | ----------- | ------ | ------------------- |
| outcome | Outcome of the flush attempt. | Str: ``failure``, ``success`` | - |
| table | The buffered table name (signal type, e.g. otel_traces, otel_metrics_gauge). | Any Str | - |

### otelcol_exporter_iceberg_buffer_flushes

Number of flush attempts for a table.

| Unit | Metric Type | Value Type | Monotonic | Stability |
| ---- | ----------- | ---------- | --------- | --------- |
| {flush} | Sum | Int | true | Alpha |

#### Attributes

| Name | Description | Values | Semantic Convention |
| ---- | ----------- | ------ | ------------------- |
| outcome | Outcome of the flush attempt. | Str: ``failure``, ``success`` | - |
| table | The buffered table name (signal type, e.g. otel_traces, otel_metrics_gauge). | Any Str | - |

### otelcol_exporter_iceberg_buffer_pending_bytes

Total disk bytes across pending files for a table. Always 0 when the in-memory buffer is in use.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| By | Gauge | Int | Alpha |

#### Attributes

| Name | Description | Values | Semantic Convention |
| ---- | ----------- | ------ | ------------------- |
| table | The buffered table name (signal type, e.g. otel_traces, otel_metrics_gauge). | Any Str | - |

### otelcol_exporter_iceberg_buffer_pending_files

Number of disk-backed pending files awaiting flush for a table. Always 0 when the in-memory buffer is in use.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| {file} | Gauge | Int | Alpha |

#### Attributes

| Name | Description | Values | Semantic Convention |
| ---- | ----------- | ------ | ------------------- |
| table | The buffered table name (signal type, e.g. otel_traces, otel_metrics_gauge). | Any Str | - |

### otelcol_exporter_iceberg_buffer_pending_oldest_age_seconds

Age of the oldest pending file for a table, in seconds. Always 0 when no pending files exist or when the in-memory buffer is in use.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| s | Gauge | Double | Alpha |

#### Attributes

| Name | Description | Values | Semantic Convention |
| ---- | ----------- | ------ | ------------------- |
| table | The buffered table name (signal type, e.g. otel_traces, otel_metrics_gauge). | Any Str | - |

### otelcol_exporter_iceberg_buffer_rows

Current number of buffered rows for a table awaiting flush.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| {row} | Gauge | Int | Alpha |

#### Attributes

| Name | Description | Values | Semantic Convention |
| ---- | ----------- | ------ | ------------------- |
| table | The buffered table name (signal type, e.g. otel_traces, otel_metrics_gauge). | Any Str | - |
